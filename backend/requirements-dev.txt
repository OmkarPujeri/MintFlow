# Test-only deps — kept out of the prod image (requirements.txt).
# Install with:  pip install -r requirements.txt -r requirements-dev.txt
pytest
httpx          # required by fastapi.testclient
fakeredis      # in-memory Redis so tests need no running Redis
